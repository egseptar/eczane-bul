# Git Workflow Rules

- Whenever the user requests `git push`, perform the following workflow sequentially:
  1. Stage all changes: `git add .`
  2. Commit changes with a descriptive summary: `git commit -m "<yapılan değişikliğin açıklaması>"`
  3. Push to GitHub: `git push origin main`
